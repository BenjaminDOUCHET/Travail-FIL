package board;
/*
 * import utils.UnknowCoordinatesException;
 * 
 * public class BoardTest {
 * 
 * @Test() public void testGetTile() { Board board = new Board(4, 4) ; try {
 * board.getTile(-1, 1); fail("X coordinate must be positive"); } catch
 * (UnknowCoordinatesException e) { } try { board.getTile(6, 1);
 * fail("X coordinate must be less than board length"); } catch
 * (UnknowCoordinatesException e) {} try { board.getTile(1, -1);
 * fail("Y coordinate must be positive"); } catch (UnknowCoordinatesException e)
 * {} try { board.getTile(1, 6);
 * fail("Y coordinate must be less than board width"); } catch
 * (UnknowCoordinatesException e) {}
 * 
 * try { assertNull(board.getTile(1, 1)); } catch (UnknowCoordinatesException e)
 * { fail("getTile should return a tile");
 * 
 * } }
 * 
 * @Test public void testGetBoardWidth() { Board board = new Board(4, 4) ;
 * assertEquals(4,board.getBoardWidth()); board = new Board(6, 6);
 * assertEquals(6, board.getBoardWidth()); }
 * 
 * @Test public void testGetBoardLength() { Board board = new Board(4, 4) ;
 * assertEquals(4,board.getBoardLength()); board = new Board(6, 6);
 * assertEquals(6, board.getBoardLength()); }
 * 
 * @Test public void testRemoveAvaible() {
 * 
 * }
 * 
 * }
 */